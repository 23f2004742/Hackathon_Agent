# Requirements Audit

> **Generated by the simulated backend.** Structure is real and contract-checked; the content is scaffolding, not researched analysis. Re-run with `ANTHROPIC_API_KEY` set to have a model author this.


**Owner:** Requirements Auditor (`requirements_auditor`, validation team)  
**Produced:** 2026-09-04  
**Contract:** VALIDATION/requirements_audit.md

## Purpose

Check every requirement against what actually exists on disk.

## Satisfied

This section is required by the artifact contract for `VALIDATION/requirements_audit.md`; the Requirements Auditor must address **satisfied** before this task can pass validation. In a model-backed run this is where the analysis of satisfied lives, grounded in the inputs listed below rather than in assumption.

## Missing

This section is required by the artifact contract for `VALIDATION/requirements_audit.md`; the Requirements Auditor must address **missing** before this task can pass validation. In a model-backed run this is where the analysis of missing lives, grounded in the inputs listed below rather than in assumption.

## Evidence

This section is required by the artifact contract for `VALIDATION/requirements_audit.md`; the Requirements Auditor must address **evidence** before this task can pass validation. In a model-backed run this is where the analysis of evidence lives, grounded in the inputs listed below rather than in assumption.

## Judging Criteri

This section is required by the artifact contract for `VALIDATION/requirements_audit.md`; the Requirements Auditor must address **judging criteri** before this task can pass validation. In a model-backed run this is where the analysis of judging criteri lives, grounded in the inputs listed below rather than in assumption.

## Inputs consulted

- `PRODUCT/requirements.md` — read; opens with: # Requirements

## Prior art consulted

- knowledge_search not available to this specialist.

## Limitations

- Produced without a model; treat every statement above as a placeholder.
- No external sources were consulted, so nothing here is citable.
- The structure, file location and validation status ARE meaningful:
  this document passed its declared postconditions.
