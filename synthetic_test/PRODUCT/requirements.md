# Requirements

> **Generated by the simulated backend.** Structure is real and contract-checked; the content is scaffolding, not researched analysis. Re-run with `ANTHROPIC_API_KEY` set to have a model author this.


**Owner:** Requirements Analyst (`requirements_analyst`, product team)  
**Produced:** 2026-09-04  
**Contract:** PRODUCT/requirements.md

## Purpose

Turn the problem statement into an unambiguous, checkable contract.

## Functional Requirement

This section is required by the artifact contract for `PRODUCT/requirements.md`; the Requirements Analyst must address **functional requirement** before this task can pass validation. In a model-backed run this is where the analysis of functional requirement lives, grounded in the inputs listed below rather than in assumption.

## Non-Functional

This section is required by the artifact contract for `PRODUCT/requirements.md`; the Requirements Analyst must address **non-functional** before this task can pass validation. In a model-backed run this is where the analysis of non-functional lives, grounded in the inputs listed below rather than in assumption.

## Constraint

This section is required by the artifact contract for `PRODUCT/requirements.md`; the Requirements Analyst must address **constraint** before this task can pass validation. In a model-backed run this is where the analysis of constraint lives, grounded in the inputs listed below rather than in assumption.

## Acceptance Criteria

This section is required by the artifact contract for `PRODUCT/requirements.md`; the Requirements Analyst must address **acceptance criteria** before this task can pass validation. In a model-backed run this is where the analysis of acceptance criteria lives, grounded in the inputs listed below rather than in assumption.

## Judging Criteria

This section is required by the artifact contract for `PRODUCT/requirements.md`; the Requirements Analyst must address **judging criteria** before this task can pass validation. In a model-backed run this is where the analysis of judging criteria lives, grounded in the inputs listed below rather than in assumption.

## Submission

This section is required by the artifact contract for `PRODUCT/requirements.md`; the Requirements Analyst must address **submission** before this task can pass validation. In a model-backed run this is where the analysis of submission lives, grounded in the inputs listed below rather than in assumption.

## Inputs consulted

- `AGENT/problem_statement.md` — read; opens with: # Problem Statement

## Prior art consulted

```
patterns.md :: **Separate the agent that tests from the agent that fixes.** The Tester has no write access to `src/`. An agent that can patch the code it is testing will make the report green rather than make the product work. Failures leave as proposed tasks for a different specialist. *Source: hackathon-agent-os · Confidence: HIGH*

[...truncated, 1 more chars]

patterns.md :: **Run the end-to-end test before believing the design.** The synthetic hackathon exposed seven defects that reading the code did not, including results being filed under the wrong task in parallel waves. A green run hid it; only a failure would have revealed it in production. *Source: hackathon-agent-os · Confidence: H

[...truncated, 5 more chars]

patterns.md :: **Not yet available:** production frontend components, a real API server, auth, DB schema, deployment config. Build fresh and index them.  ---  ## TECH
```

## Limitations

- Produced without a model; treat every statement above as a placeholder.
- No external sources were consulted, so nothing here is citable.
- The structure, file location and validation status ARE meaningful:
  this document passed its declared postconditions.
