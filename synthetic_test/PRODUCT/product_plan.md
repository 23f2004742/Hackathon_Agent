# Product Plan

> **Generated by the simulated backend.** Structure is real and contract-checked; the content is scaffolding, not researched analysis. Re-run with `ANTHROPIC_API_KEY` set to have a model author this.


**Owner:** Product Manager (`product_manager`, product team)  
**Produced:** 2026-09-04  
**Contract:** PRODUCT/product_plan.md

## Purpose

Decide what actually gets built in the time available, and what does not.

## Must Have

This section is required by the artifact contract for `PRODUCT/product_plan.md`; the Product Manager must address **must have** before this task can pass validation. In a model-backed run this is where the analysis of must have lives, grounded in the inputs listed below rather than in assumption.

## Should Have

This section is required by the artifact contract for `PRODUCT/product_plan.md`; the Product Manager must address **should have** before this task can pass validation. In a model-backed run this is where the analysis of should have lives, grounded in the inputs listed below rather than in assumption.

## Nice To Have

This section is required by the artifact contract for `PRODUCT/product_plan.md`; the Product Manager must address **nice to have** before this task can pass validation. In a model-backed run this is where the analysis of nice to have lives, grounded in the inputs listed below rather than in assumption.

## Do Not Build

This section is required by the artifact contract for `PRODUCT/product_plan.md`; the Product Manager must address **do not build** before this task can pass validation. In a model-backed run this is where the analysis of do not build lives, grounded in the inputs listed below rather than in assumption.

## Mvp

This section is required by the artifact contract for `PRODUCT/product_plan.md`; the Product Manager must address **mvp** before this task can pass validation. In a model-backed run this is where the analysis of mvp lives, grounded in the inputs listed below rather than in assumption.

## User Stor

This section is required by the artifact contract for `PRODUCT/product_plan.md`; the Product Manager must address **user stor** before this task can pass validation. In a model-backed run this is where the analysis of user stor lives, grounded in the inputs listed below rather than in assumption.

## Success Metric

This section is required by the artifact contract for `PRODUCT/product_plan.md`; the Product Manager must address **success metric** before this task can pass validation. In a model-backed run this is where the analysis of success metric lives, grounded in the inputs listed below rather than in assumption.

## Inputs consulted

- `PRODUCT/requirements.md` — read; opens with: # Requirements

## Prior art consulted

```
patterns.md :: **Separate the agent that tests from the agent that fixes.** The Tester has no write access to `src/`. An agent that can patch the code it is testing will make the report green rather than make the product work. Failures leave as proposed tasks for a different specialist. *Source: hackathon-agent-os · Confidence: HIGH*

[...truncated, 1 more chars]

patterns.md :: **Run the end-to-end test before believing the design.** The synthetic hackathon exposed seven defects that reading the code did not, including results being filed under the wrong task in parallel waves. A green run hid it; only a failure would have revealed it in production. *Source: hackathon-agent-os · Confidence: H

[...truncated, 5 more chars]

patterns.md :: **Not yet available:** production frontend components, a real API server, auth, DB schema, deployment config. Build fresh and index them.  ---  ## TECH
```

## Limitations

- Produced without a model; treat every statement above as a placeholder.
- No external sources were consulted, so nothing here is citable.
- The structure, file location and validation status ARE meaningful:
  this document passed its declared postconditions.
