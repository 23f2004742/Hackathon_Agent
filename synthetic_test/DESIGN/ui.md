# Ui

> **Generated by the simulated backend.** Structure is real and contract-checked; the content is scaffolding, not researched analysis. Re-run with `ANTHROPIC_API_KEY` set to have a model author this.


**Owner:** UI Designer (`ui_designer`, design team)  
**Produced:** 2026-09-04  
**Contract:** DESIGN/ui.md

## Purpose

Define the visual system: hierarchy, type scale, spacing, colour, components
and responsive behaviour.

## Typography

This section is required by the artifact contract for `DESIGN/ui.md`; the UI Designer must address **typography** before this task can pass validation. In a model-backed run this is where the analysis of typography lives, grounded in the inputs listed below rather than in assumption.

## Spacing

This section is required by the artifact contract for `DESIGN/ui.md`; the UI Designer must address **spacing** before this task can pass validation. In a model-backed run this is where the analysis of spacing lives, grounded in the inputs listed below rather than in assumption.

## Color

This section is required by the artifact contract for `DESIGN/ui.md`; the UI Designer must address **color** before this task can pass validation. In a model-backed run this is where the analysis of color lives, grounded in the inputs listed below rather than in assumption.

## Component

This section is required by the artifact contract for `DESIGN/ui.md`; the UI Designer must address **component** before this task can pass validation. In a model-backed run this is where the analysis of component lives, grounded in the inputs listed below rather than in assumption.

## Responsive

This section is required by the artifact contract for `DESIGN/ui.md`; the UI Designer must address **responsive** before this task can pass validation. In a model-backed run this is where the analysis of responsive lives, grounded in the inputs listed below rather than in assumption.

## Contrast

This section is required by the artifact contract for `DESIGN/ui.md`; the UI Designer must address **contrast** before this task can pass validation. In a model-backed run this is where the analysis of contrast lives, grounded in the inputs listed below rather than in assumption.

## Inputs consulted

- `DESIGN/ux.md` — read; opens with: # Ux

## Prior art consulted

```
patterns.md :: **Run the end-to-end test before believing the design.** The synthetic hackathon exposed seven defects that reading the code did not, including results being filed under the wrong task in parallel waves. A green run hid it; only a failure would have revealed it in production. *Source: hackathon-agent-os · Confidence: H

[...truncated, 5 more chars]

patterns.md :: **Prevent leakage structurally, then prove it empirically.** Truncation happened once, inside `load_observation_window()`, so future days physically could not reach a feature. Section 5 then rebuilt features from files with the risk window deleted and confirmed identical output. Design plus proof beats either alone. *S

[...truncated, 49 more chars]
```

## Limitations

- Produced without a model; treat every statement above as a placeholder.
- No external sources were consulted, so nothing here is citable.
- The structure, file location and validation status ARE meaningful:
  this document passed its declared postconditions.
