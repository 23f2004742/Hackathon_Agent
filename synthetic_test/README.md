# Readme

> **Generated by the simulated backend.** Structure is real and contract-checked; the content is scaffolding, not researched analysis. Re-run with `ANTHROPIC_API_KEY` set to have a model author this.


**Owner:** Technical Writer (`technical_writer`, communication team)  
**Produced:** 2026-09-04  
**Contract:** README.md

## Purpose

Document what was actually built. Read the code; do not paraphrase the plan.

## Install

This section is required by the artifact contract for `README.md`; the Technical Writer must address **install** before this task can pass validation. In a model-backed run this is where the analysis of install lives, grounded in the inputs listed below rather than in assumption.

## Run

This section is required by the artifact contract for `README.md`; the Technical Writer must address **run** before this task can pass validation. In a model-backed run this is where the analysis of run lives, grounded in the inputs listed below rather than in assumption.

## What It Does

This section is required by the artifact contract for `README.md`; the Technical Writer must address **what it does** before this task can pass validation. In a model-backed run this is where the analysis of what it does lives, grounded in the inputs listed below rather than in assumption.

## Inputs consulted

- `PRODUCT/architecture.md` — read; opens with: # Architecture

## Prior art consulted

- knowledge_search not available to this specialist.

## Limitations

- Produced without a model; treat every statement above as a placeholder.
- No external sources were consulted, so nothing here is citable.
- The structure, file location and validation status ARE meaningful:
  this document passed its declared postconditions.
