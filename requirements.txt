# Pinned lower bounds: a judge rebuilding this should get a working tree.
# The Agent SDK is the one that matters: it runs the specialists on your own
# Claude subscription. `anthropic` is only needed for the tool schemas and the
# opt-in paid backend.
claude-agent-sdk>=0.2.152
anthropic>=1.3.0
requests>=2.32.0
python-pptx>=1.0.2
reportlab>=4.0.0
openpyxl>=3.1.0
pytest>=8.0.0
